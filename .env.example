# Backend environment variables for local development.
# Generate every secret independently; never reuse these placeholder strings.
MONGO_URI=mongodb://127.0.0.1:27017/hitrack
<<<<<<< HEAD
jinjn
=======
NODE_ENV=development
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
JWT_EXPIRE=7d
CLIENT_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173
PORT=5000
SETUP_SECRET=replace-with-an-independent-random-setup-secret
ABOUT_ME_SECRET=replace-with-an-independent-random-unlock-secret
# Set to true only for temporary local CORS troubleshooting.
ENABLE_CORS_DEBUG=false
# Optional initial-connect recovery tuning (milliseconds).
DB_RECONNECT_INTERVAL_MS=15000
# Per-IP API request allowance per 15 minutes; auth routes stay capped at 10.
GENERAL_RATE_LIMIT_MAX=2000
>>>>>>> 2cf13c8 (refactor scoring engine and harden HiTrack platform)
